# 站点 URL（本地开发用）
NEXT_PUBLIC_SITE_URL="http://localhost:3000"

# basePath 配置（按部署环境设置，留空为根路径部署）：
# - Cloudflare Pages / Vercel / 本地开发 / Electron：留空（不设置）
# - GitHub Pages（仓库名为 person）：设置为 /person
# 注意：此变量通常在 CI/CD 平台环境变量中设置，不放在 .env 中
